# 更新日誌

本檔案記錄各版本的變更。

## [1.3.0] - 2026-06-14

### 新增
- 下載「停滯偵測」：連續 15 分鐘沒有下載進度時，自動判定卡住並停止，並提示檢查網路（與原本的 6 小時總時限並存）。
- 新增「關於」對話框（選單列 說明 → 關於）：顯示版本、專案連結、核心下載器署名與第三方授權。
- 從原始碼執行時，啟動前會檢查必要套件，缺少時一次列出並附上安裝指令。

### 修正
- 修正視窗標題中夾帶的隱形字元（版本字串的 BOM）。
- 修正輸入網址與預覽不符仍選擇直接下載時，背景資訊抓取結束會誤將下載狀態重設、可能重複觸發下載的問題。
- 殘留下載器的清理改為只針對本程式自己上次啟動的程序，不再可能誤關系統上其他同名程式。
- 下載超時、停滯或下載器異常結束時，不再誤顯示「完成」對話框，改為對應的提示訊息。
- 修正下載逾時提示顯示的時數與實際設定不符。
- 繁體 EPUB 的語言標籤改為 zh-TW（閱讀器斷詞與字型更正確）。
- 一次偵測到多個下載資料夾時改為全部處理（原本只處理第一個）。
- 歷史紀錄選單改用 tk_popup，點選單外側可正確關閉。

### 變更
- 內部重構：將單一檔案拆分為 core / scraper / epub_tools / packer / gui 多模組（行為不變）。
- 將 PyInstaller 設定檔 `.spec` 納入版本控制。

### 其他
- 內建下載器 bili_novel_packer 更新至 0.2.43。
- 隨發行附上第三方套件與上游下載器（bili_novel_packer）的授權聲明（`NOTICES.txt`、`THIRD_PARTY_LICENSES.txt`、`tools/LICENSE-bili_novel_packer.txt`）。

## [1.2.0] - 2026-05-23

### 新增
- 防多開機制與視窗喚醒：重複開啟時自動喚回既有視窗至最前層。

### 變更
- 專案更名為 BiliNovelDownloader（移除 GUI 字尾）。
- 下載完成的資料夾偵測改用「操作前後目錄快照差集」，解決暫存區有多個資料夾時的誤判。
- README 排版優化；`requirements.txt` 為依賴套件加上最低版本限制。

### 修正
- 轉繁體前新增 EPUB 檔案大小檢查，遇損壞檔直接報錯，不再丟出不明確的錯誤。
- 強化殘留下載器的清理（比對命令列），解決 Windows 進程名稱過長被截斷而無法終止的問題。
- 修正非同步更新介面時的閉包錯誤，使封面下載或目錄抓取的錯誤訊息能正確顯示。
- 歷史紀錄讀取失敗時不再靜默忽略，會記錄並重置。

## [1.1.1] - 2026-05-12

### 變更
- 下載總結中的書名改以繁體顯示。
- 更新程式圖示。

## [1.1.0] - 2026-05-11

### 新增
- 即時讀取下載器日誌並顯示於介面，底層錯誤不再被吞掉。
- 下載歷史紀錄：自動記住最近成功載入的網址與書名，可從下拉選單快速重載；點選後自動載入資訊與封面。
- 封面圖片 24 小時快取，加快二次載入。
- 下載完成後顯示摘要（書名、EPUB 數量、耗時）。
- 友善的錯誤提示分類（找不到下載器、網路斷線、查無小說等）。
- 網址格式驗證，提早阻擋錯誤輸入。

### 變更
- 改用自製進度條，下載時介面不再凍結。
- 視窗可自由縮放、版面可拖曳調整，資訊欄位隨視窗寬度自動填滿。
- 整理專案結構（新增 `assets/`、`requirements.txt`、`version.txt` 動態版號）。

### 修正
- EPUB 轉換加入暫存檔檢查，避免轉換失敗時覆蓋並遺失原檔。
- 修正清空暫存時誤刪封面快取導致的錯誤。
- 修正載入中重複點擊「下載／檢查」造成的狀態衝突。
- 修正特定字體下破折號顯示為斷裂橫線的問題。
- 修正輸入數字 ID 後游標停在開頭的小瑕疵。

## [1.0.0] - 2026-04-22

- Initial Release