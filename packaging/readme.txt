# 使用須知

1. 請先解壓縮整個壓縮檔，勿在 ZIP 內直接執行
2. 請保持 `BiliNovelDownloader.exe` 與 `tools` 資料夾在「同一層目錄」; (若想放在桌面，可對 exe 按右鍵建立捷徑，勿單獨把 exe 移走)
3. 若未來下載器有新版本可用，請至 bili_novel_packer 的 GitHub (https://github.com/Montaro2017/bili_novel_packer) 下載最新 exe，替換掉 tools 資料夾裡面的舊檔案即可